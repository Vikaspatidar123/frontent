const projectName = 'ARC';

const role = 'Admin';

const footerText = 'Design and powered by GAMMASTACK';

const tableCustomClass = '';
// const tableCustomClass = 'table-dark'; // for dark table
// const tableCustomClass = 'table-striped-columns'; // for stripped columns (zebra style columns)
// const tableCustomClass = 'table-bordered border-success' // For changing color of border

const theadClass = 'table-light'; // for grey header

const tbodyClass = '';
// const tbodyClass = 'table-striped' // for stripped rows (zebra style columns)

const itemsPerPage = 10;

const PER_PAGE = 15;

const debounceTime = 700;

const selectedLanguage = 'EN';

const YMDFormat = 'Do MMM YYYY';

const dateTimeFormat = `${YMDFormat} hh:mm`;

const dateTimeFormatWithA = `${YMDFormat} hh:mm A`;

const YMDdate = 'YYYY-MM-DD';

const defaultCurrencyId = '1';

export {
	projectName,
	role,
	footerText,
	tableCustomClass,
	theadClass,
	tbodyClass,
	itemsPerPage,
	debounceTime,
	selectedLanguage,
	YMDFormat,
	dateTimeFormat,
	YMDdate,
	dateTimeFormatWithA,
	defaultCurrencyId,
	PER_PAGE,
};

const Keys = (cell) => (cell.value ? cell.value : '');

const English = (cell) => (cell.value ? cell.value : '');

const Action = (cell) => (cell.value ? cell.value : '');

export { Action, Keys, English };

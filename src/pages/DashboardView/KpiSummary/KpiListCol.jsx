// import React from 'react';
// import { Badge } from 'reactstrap';
// import { Link } from 'react-router-dom';
// import PropTypes from 'prop-types';

const RowName = ({ cell }) => (cell || 0);

const Today = ({ cell }) => (cell || 0);

const Yesterday = ({ cell }) => (cell || 0);

const MonthToDate = ({ cell }) => (cell || 0);

const CustomDate = ({ cell }) => (cell || 0);

const Delta = ({ cell }) => (cell || 0);

export { RowName, Today, Yesterday, MonthToDate, CustomDate, Delta };
